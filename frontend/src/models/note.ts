export interface Note {
    _id: string,
    title : string,
    discription? : string,
    priority? : string,
    statuss? : string,
    dueDate?:  string,
    createdAt: string,
    updatedAt: string
}